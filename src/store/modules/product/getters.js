export default {
    box(state) {
        return state.box;
    }
    , card(state) {
        return state.card;
    }
    , gifts(state) {
        return state.gifts;
    }
    , giftBoxes(state) {
        return state.giftBoxes;
    }
    , message(state) {
        return state.message;
    }, price(state) {
        return state.price;
    },
}
